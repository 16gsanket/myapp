Install -> mongoDB, mongoDB COMPASS , nodeJS

in VS Code Terminal ->  1)npm i nodemon -g 
        ----ensure mongoDB is installed -----
                        2) to run server 
                            | 
                            |-> npx nodemon